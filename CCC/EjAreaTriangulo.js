
function area(){
    let base, altura, resultado;

    base=parseInt(prompt("introduce una base"));
    altura=parseInt(prompt("introduce una altura"));
    resultado=(base*altura)/2;
    
    return alert("el area es " + resultado);
}